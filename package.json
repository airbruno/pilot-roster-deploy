{
  "name": "pilot-roster-deploy",
  "private": true,
  "scripts": {
    "sync:frontend": "node scripts/sync-frontend.mjs",
    "check:static": "node scripts/check-static.mjs",
    "check:parser": "node scripts/check-parser-fixtures.mjs",
    "check:journeys": "node scripts/check-journey-render.mjs",
    "check:functions": "node --check functions/index.js",
    "check:visual": "node scripts/visual-check.mjs",
    "predeploy": "npm run check:static && npm run check:parser && npm run check:journeys && npm run check:functions",
    "deploy:firebase": "firebase deploy"
  }
}
